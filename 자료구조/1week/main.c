#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[]) {
	// 데이터 저장/인출 방법
	// 1. 저장장소(메모리) 예약(변수 선언) : 1. 데이터 종류, 2. 데이터 최대크기, 3. 예약자 이름(한글x, 영어, 숫자). 
	// 2. 변수에 데이터 저장 : "="연산자 (연산자 우측(데이터) --> 좌측 저장: 변수) 
	// 3. 변수로부터 데이터 꺼내기 : 변수이름 부르기. 
		
	
	// 여러개 중에 하나만 선택하기
	
	// Y/N로 답할수 있는 질문을 던져서, 그 결과에 따라, 선택됨.
	
	 // 예) 양수, 음수 출력.
	 
	  
	
	
	return 0;
}
